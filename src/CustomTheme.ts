import { createTheme } from '@mui/material/styles';

const colorTheme = createTheme({
  palette: {
    background: {
      default: '#F5F5F5',
    },
    primary: {
      main: '#0044B0',
    },
    secondary: {
      main: '#D9D9D9',
    },
  },
});

export default colorTheme;
